# HURL Configuration for Unburdy Server Client Management Tests

# Server host configuration
host = http://localhost:8080

# Test configuration
default_timeout = 30
max_retries = 3

# Environment settings for pagination testing
# These should match the values in .env:
# MAX_PAGE_LIMIT=1000
# DEFAULT_PAGE_LIMIT=200